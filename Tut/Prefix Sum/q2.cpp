// In the name of Allah, the Most Gracious, the Most Merciful

/**
 * [HackerRank] Array Manipulation
 *
 * You are given an array a[N] and Q updates.
 * An update consists of three numbers L, R and X. You need to increase all elements at indices L...R by +x.
 * Print the final sequence
 * N, Q <= 10^5
 */

#include <bits/stdc++.h>
using namespace std;
#define test     \
    long long t; \
    cin >> t;    \
    while (t--)

int32_t main()
{
    // Fast I/O
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    test
    {
    }
}