// In the name of Allah, the Most Gracious, the Most Merciful
#include <bits/stdc++.h>

using ll = long long;
using ull = unsigned long long;
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n, m;
        cin >> n >> m;
        int arr[n][m];
        vector<int> p;

        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < m; i++)
            {
                cin >> arr[i][j];
            }
        }
    }
}