#include <iostream> // In the name of Allah, the Most Gracious, the Most Merciful
using namespace std;

int main()
{
    int friends, bottles, milliliters, limes, slices, gram, drinkMl, saltG, toastCount = 0;

    cin >> friends >> bottles >> milliliters >> limes >> slices >> gram >> drinkMl >> saltG;

    if (true)
    {
    }

    return 0;
}