// In the name of Allah, the Most Gracious, the Most Merciful
#include <bits/stdc++.h>

using ll = long long;
using ull = unsigned long long;
using namespace std;

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n, s = 0, point = 0;
        cin >> n;
        vector<int> v(n);

        for (int i = 0; i < n; i++)
        {
            int value;
            cin >> value;
            v[i] = value;
        }
    }
}